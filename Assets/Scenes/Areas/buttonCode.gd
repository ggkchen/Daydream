extends Button

# func _ready():
	# Connect the pressed signal
   # connect("pressed", self, "_on_button_pressed")

func _on_button_pressed():
	# Change scene to Gambling
	get_tree().change_scene("  ")  # replace path if needed
